# Гайд Разработчика для RAG Модуля 🔨

**Версия:** 1.0.0  
**Статус:** В разработке  
**Последнее обновление:** 2025-12-21  

---

## 🚀 Начало Работы

### 1. Подготовка Окружения

```bash
# Клонируем репо
git clone https://github.com/severand/TG_bot.git
cd TG_bot

# Переходим на фичер ветку
git checkout feature/rag-knowledge-base

# Создаем Python окружение
python -m venv venv
source venv/bin/activate  # или `venv\Scripts\activate` на Windows

# Устанавливаем зависимости RAG
cd rag_knowledge_base
pip install -r requirements.txt
pip install -e .  # Устанавливаем модуль в режиме редактирования

# Проверяем установку
python -c "import rag_module; print('✅ RAG модуль готов')"
```

### 2. Структура Проекта

```
rag_knowledge_base/
├── rag_module/              # Основной модуль
│   ├── services/            # Основные сервисы
│   ├── utils/               # Утилиты
│   ├── config.py            # Конфигурация
│   ├── models.py            # Модели данных
│   └── exceptions.py        # Исключения
├── tests/                   # Тесты
├── examples/                # Примеры
├── docs/                    # Расширенная документация
└── requirements.txt         # Зависимости
```

---

## 📘 Фазы Реализации

### Фаза 1: Основные Сервисы (Дни 1-2)

**Цель:** Построить RAG pipeline

#### День 1: Фундамент

1. **Создать базовые структуры**
   ```bash
   touch rag_module/__init__.py
   touch rag_module/config.py
   touch rag_module/models.py
   touch rag_module/exceptions.py
   mkdir -p rag_module/services
   mkdir -p rag_module/utils
   ```

2. **Реализовать `config.py`**
   - Загрузка из переменных окружения
   - Разумные значения по умолчанию
   - Валидация при запуске

3. **Реализовать `models.py`**
   - Dataclasses: Document, Chunk, SearchResult
   - Методы валидации
   - Помощники сериализации

4. **Реализовать `exceptions.py`**
   - Базовый RAGException
   - Специфичные исключения

5. **Написать тесты** (`tests/test_models.py`)
   - Создание моделей
   - Сериализация
   - Валидация

#### День 2: Основные Сервисы

1. **`services/chunker.py`** (3 часа)
   - Алгоритм разбивки текста
   - Обработка overlap
   - Обработка метаданных
   - Тесты: `tests/test_chunker.py`

2. **`services/embeddings.py`** (2 часа)
   - Загрузка Sentence-Transformers
   - Embedding одного текста
   - Batch embedding
   - Валидация размерности
   - Тесты: `tests/test_embeddings.py`

3. **`services/vector_store.py`** (2 часа)
   - Инициализация ChromaDB
   - Операции: добавить/удалить/поиск
   - Обработка метаданных
   - Тесты: `tests/test_vector_store.py`

4. **`services/file_processor.py`** (3 часа)
   - Извлечение текста из PDF (используем PDFParser)
   - Извлечение из DOCX (используем DOCXParser)
   - Поддержка TXT
   - Обработка ошибок
   - Тесты: `tests/test_file_processor.py`

### Фаза 2: Интеграция (Дни 3-4)

1. **`services/retriever.py`** (2 часа)
   - Объединение embeddings + vector store
   - Семантический поиск
   - Поддержка фильтрации
   - Тесты: `tests/test_retriever.py`

2. **`services/manager.py`** (4 часа)
   - Координация всех сервисов
   - High-level API
   - Обработка ошибок
   - Логирование
   - Тесты: `tests/test_manager.py` + интеграция

3. **Интеграция с TG_bot**
   - Создать `app/handlers/knowledge.py`
   - Создать `app/states/knowledge.py`
   - Обновить `main.py` для регистрации handler
   - Обновить `requirements.txt` в основном проекте

### Фаза 3: Документация & Примеры (День 5)

1. Написать API документацию (`docs/API.md`)
2. Создать примеры использования (`examples/`)
3. Написать FAQ (`docs/FAQ.md`)
4. Бенчмарки производительности (`docs/PERFORMANCE.md`)

---

## 🔬 Стандарты Кода

### Style Guide

**Используем:** Type hints Python 3.10+

```python
# ✅ Правильно
async def add_document(
    self,
    file_path: Path,
    doc_id: str,
    metadata: Dict[str, Any] | None = None
) -> DocumentInfo:
    """Добавить документ в базу знаний.
    
    Args:
        file_path: Путь к файлу документа
        doc_id: Уникальный идентификатор документа
        metadata: Опциональный словарь метаданных
        
    Returns:
        Информация о добавленном документе
        
    Raises:
        FileNotFoundError: Если файл не существует
        RAGException: Если обработка не удалась
    """
    pass
```

### Docstring Формат

Используем Google-style docstrings:

```python
def process_chunk(text: str, size: int = 500) -> List[str]:
    """Разбить текст на куски указанного размера.
    
    Args:
        text: Входной текст для разбивки
        size: Макс размер каждого куска (по умолчанию 500)
        
    Returns:
        Список текстовых кусков
        
    Raises:
        ValueError: Если size <= 0
    """
    pass
```

### Логирование

```python
import logging

logger = logging.getLogger(__name__)

# ✅ Используйте структурированное логирование
logger.info("Документ добавлен", extra={
    "doc_id": doc_id,
    "chunk_count": len(chunks),
    "duration_ms": elapsed_ms
})

# ❌ Избегайте интерполяции строк
logger.info(f"Документ {doc_id} добавлен")  # Плохо - невозможно фильтровать
```

---

## 🧪 Тестирование

### Запуск Тестов

```bash
# Все тесты
pytest tests/

# С покрытием кода
pytest tests/ --cov=rag_module --cov-report=html

# Конкретный файл тестов
pytest tests/test_chunker.py -v

# Конкретный тест
pytest tests/test_chunker.py::TestChunker::test_chunk_text -v

# Watch режим (автоперезапуск при изменениях)
pytest-watch tests/
```

### Структура Тестов

```python
# tests/test_chunker.py
import pytest
from rag_module.services.chunker import Chunker
from rag_module.models import Chunk

class TestChunker:
    """Набор тестов для сервиса Chunker."""
    
    @pytest.fixture
    def chunker(self):
        """Создать экземпляр chunker для тестов."""
        return Chunker(chunk_size=500, overlap=50)
    
    def test_chunk_simple_text(self, chunker):
        """Тестировать разбивку простого текста."""
        text = "Привет мир. Это тест."
        chunks = chunker.chunk_text(text, {"source": "test"})
        
        assert len(chunks) > 0
        assert all(isinstance(c, Chunk) for c in chunks)
```

### Требования Покрытия

- **Сервисы:** >80% покрытие
- **Утилиты:** >70% покрытие
- **Модели:** >90% покрытие (критичные)
- **Общее:** >75% целевое значение

---

## 🖥️ Git Workflow

### Стратегия Веток

```
feature/rag-knowledge-base          # Основная фичер ветка
  ├── feature/rag-chunker          # Отдельные сервисы
  ├── feature/rag-embeddings
  ├── feature/rag-vector-store
  ├── feature/rag-retriever
  ├── feature/rag-manager
  └── feature/rag-integration      # Интеграция с TG_bot
```

### Commit Messages

Используем conventional commits:

```
feat(rag/chunker): реализовать алгоритм разбивки текста
  - Поддержка настраиваемого размера chunks
  - Обработка overlap
  - Unit тесты

fix(rag/embeddings): исправить обработку больших батчей
  - Оптимизация памяти для батчей > 1000
  - Добавлена валидация размера батча
  - Обновлены тесты

docs(rag): обновить API документацию
  - Добавлены примеры для каждого сервиса
  - Уточнена обработка ошибок

test(rag): добавить интеграционные тесты
  - Полный pipeline тест (загрузка -> поиск)
  - Бенчмарки производительности
```

---

## 🗑️ Получить Помощь

Если застрял:

1. Проверь **TROUBLESHOOTING.md**
2. Посмотри **docs/FAQ.md**
3. Посмотри похожие тесты для примеров
4. Проверь ARCHITECTURE.md для решений проектирования
5. Создай GitHub issue с деталями

---

**Счастливой разработки! 🚀**

**Последнее обновление:** 2025-12-21  
**Ответственный:** Владелец проекта
