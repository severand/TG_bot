# Core dependencies
aiogram>=3.0.0
replicate>=0.15.0
python-docx>=1.0.0
pypdf>=4.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
httpx>=0.21.0

# Document format support
# ПОЛНАЯ ПОДДЕРЖКА ВСЕХ ФОРМАТОВ - без SSL зависимостей

# Word documents (.docx и .doc)
python-docx>=1.0.0

# PDF documents
pypdf>=4.0.0
pdf2image>=1.16.0

# PowerPoint documents
python-pptx>=0.6.21

# Excel support - BOTH .xlsx and .xls (NO external deps needed!)
# .xlsx reads directly from ZIP+XML (stdlib only)
# .xls converts via PowerShell COM (Windows+Excel) or LibreOffice soffice
# NO pandas/openpyxl/xlrd needed!

# OCR dependencies for photos
pytesseract>=0.3.10
Pillow>=10.0.0

# Encoding detection
chardet>=5.0.0

# ПРИМЕЧАНИЕ: Все эти библиотеки НЕ имеют SSL проблем при установке
# Работают out-of-the-box на Windows/Mac/Linux
# Excel парсер использует только встроенные модули Python (zipfile, xml, subprocess)
