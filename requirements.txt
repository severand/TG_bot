# Core dependencies
aiogram>=3.0.0
replicate>=0.15.0
python-docx>=1.0.0
pypdf>=4.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
httpx>=0.21.0

# Document format support
# Дата исправления: 2025-12-28 19:42
# ОСНОВНАЯ ПОДДЕРЖКА: DOCX, PDF, XLSX (современные форматы)
# ЛЕГАСИ: .doc и .xls требуют ручной установки

# Word documents (.docx) - ОСНОВНОЙ ФОРМАТ
python-docx>=1.0.0

# PDF documents
pypdf>=4.0.0
pdf2image>=1.16.0

# PowerPoint documents
python-pptx>=0.6.21

# Excel/XLSX support - ОСНОВНОЙ ФОРМАТ
openpyxl>=3.0.10
# xlrd>=2.0.0  # Для .xls (опционально, установить вручную: pip install xlrd)

# OCR dependencies
pytesseract>=0.3.10
Pillow>=10.0.0

# Encoding detection (fallback)
chardet>=5.0.0

# ПРИМЕЧАНИЕ: Для поддержки ЛЕГАСИ форматов:
# .doc -> pip install aspose-words (коммерческая библиотека)
# .xls -> pip install xlrd
# Эти зависимости опциональны и могут вызывать SSL ошибки при установке
